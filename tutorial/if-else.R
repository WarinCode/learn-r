# การใช้ if else
# โครงสร้าง

# แบบที่ 1
# if(เงื่อนไข){
#   ถ้าเงื่อนไขเป็นจริงให้ทำอะไร ...
# } else {
#   เงื่อนไขไม่เป็นจริงให้ทำอะไร ...
# }

# แบบที่ 2
# if(เงื่อนไข){
#   ถ้าเงื่อนไขเป็นจริงให้ทำอะไร ...
# } else if (เงื่อนไข) {
#   ถ้าเงื่อนไขเป็นจริงให้ทำอะไร ...
# } else {
#   เงื่อนไขไม่เป็นจริงให้ทำอะไร ...
# }